<?xml version="1.0" encoding="utf-8"?>

<!-- the root view must be the DrawerLayout -->
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".NewSettingsActivity"
    android:background="#EAE1B0"
    tools:ignore="HardcodedText">

    <!-- Content layout -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- 中部 dropdown -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp">

            <TextView
                android:id="@+id/dropdown_chubu"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="中部"
                android:textSize="24sp"
                android:padding="16dp"
                android:drawableEnd="@drawable/ic_arrow_right"
                android:drawablePadding="8dp" />
        </FrameLayout>

        <!-- Items for 中部 dropdown -->
        <LinearLayout
            android:id="@+id/items_chubu"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="32dp"
            android:visibility="gone"
            android:layout_weight="1">

            <!-- 富山弁 row -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="start|center_vertical"
                android:layout_marginBottom="8dp">

                <TextView
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="富山弁"
                    android:textSize="16sp"
                    android:gravity="start" />

                <!-- Rounded rectangle button for mode -->
                <Button
                    android:id="@+id/mode_button_toyama"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="学習"
                    android:background="@drawable/rounded_rectangle_button" />

                <!-- Circle button for toggle -->
                <Button
                    android:id="@+id/toggle_button_toyama"
                    android:layout_width="40dp"
                    android:layout_height="40dp"
                    android:text="-"
                    android:background="@drawable/round_button"
                    android:layout_marginStart="8dp" />
            </LinearLayout>

            <!-- 飛騨弁 row -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="start|center_vertical">

                <TextView
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="飛騨弁"
                    android:textSize="16sp"
                    android:gravity="start" />

                <!-- Rounded rectangle button for mode -->
                <Button
                    android:id="@+id/mode_button_hida"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="学習"
                    android:background="@drawable/rounded_rectangle_button" />

                <!-- Circle button for toggle -->
                <Button
                    android:id="@+id/toggle_button_hida"
                    android:layout_width="40dp"
                    android:layout_height="40dp"
                    android:text="-"
                    android:background="@drawable/round_button"
                    android:layout_marginStart="8dp" />
            </LinearLayout>
        </LinearLayout>

        <!-- 近畿 dropdown (disabled) -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="近畿 (Coming Soon)"
            android:textSize="24sp"
            android:layout_marginTop="8dp"
            android:padding="16dp"
            android:enabled="false"
            android:textColor="@android:color/darker_gray"
            android:drawableEnd="@drawable/ic_arrow_right"
            android:drawablePadding="8dp" />

<!--        <ImageView-->
<!--            android:id="@+id/backToMainButton"-->
<!--            android:layout_width="65dp"-->
<!--            android:layout_height="match_parent"-->
<!--            android:scaleType="fitEnd"-->
<!--            android:src="@drawable/ic_close" />-->
    </LinearLayout>

    <!-- Navigation view -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        app:menu="@menu/navigation_menu" />

</androidx.drawerlayout.widget.DrawerLayout>
